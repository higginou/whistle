# Retrospective Epic 12 : Enrichissement Donjon post-migration

Date : 2026-05-04
Status : done

## Epic Review

Epic 12 a restaure les essais dans le Donjon sans revenir au `scraped.json`. La donnee suit maintenant le chemin actif Vercel : cockpit admin -> API admin -> Postgres -> API publique -> Donjon.

## Ce qui a bien fonctionne

- Le cockpit collectait deja `homeTries` et `awayTries`, donc aucun changement structurel UI n'a ete necessaire.
- La story a garde le fallback Epic 11 : les anciennes donnees sans essais ne cassent pas Donjon.
- La review automatique a repere deux problemes de robustesse de contrat DB/API avant cloture.
- Les tests couvrent maintenant le contrat API, le schema DB et l'envoi cockpit.

## Corrections post-review

- `homeTries` / `awayTries` sont bornes a 0-50 cote API admin.
- Les contraintes DB sont nommees et idempotentes, meme si les colonnes existent deja.
- La migration utilise `NOT VALID` pour ne pas bloquer une base existante tout en contraignant les nouvelles ecritures.

## Verification

- `npx vitest run` : 45 fichiers, 908 tests, 0 echec.
- `npx biome check .` : 0 erreur.
- `npm run build` : build Vite/PWA OK.
- `cargo test` : non applicable, aucun dossier `src-tauri/`.

## Action Items

- [ ] Si une base existante contient deja des valeurs d'essais hors borne, nettoyer les lignes puis valider les contraintes Postgres manuellement.
- [ ] Si d'autres consommateurs que Donjon lisent `results[]`, documenter que les essais peuvent rester `null` pour les anciennes lignes.

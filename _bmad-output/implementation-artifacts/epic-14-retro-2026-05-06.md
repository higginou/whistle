# Epic 14 Retrospective : Arrivee Tribune & Classement Direct

Status: done

## Epic Review

Epic 14 a livre le nouveau flux d'arrivee : validation maquettes, carte Tribune seule au premier affichage de session, fermeture `sessionStorage`, classement direct, puis suppression du bouton de reveal.

### Resultats

- Les 3 stories sont `done` dans les fichiers story et dans `sprint-status.yaml`.
- Le gate UI a ete respecte : Story 14.1 a produit 3 variantes, higgin a donne un GO explicite sur la variante B enrichie, puis Story 14.2 a implemente cette direction.
- Le runtime reutilise la carte Tribune existante au lieu de dupliquer les calculs et textes metier.
- Le bouton `Reveler la projection`, son style, son test obsolete et l'etat store `revealed` ont ete retires du flux classement.
- Les checks finaux de Story 14.3 sont verts : `npx vitest run` (923 tests) et `npx biome check .`.
- `src-tauri/Cargo.toml` est absent, donc aucun target `cargo test` n'existe dans ce workspace.

### Ce Qui A Bien Fonctionne

- Le gate maquettes a evite d'implementer trop tot : la premiere recommandation agent etait la variante C, mais le feedback higgin a oriente la vraie solution vers la variante B avec toutes les infos.
- La reutilisation de `score-card.js` avec option `forceDetaille` a garde une seule source de verite pour les donnees Tribune.
- Les revues automatiques ont trouve des problemes concrets : liens Markdown incorrects, couverture reduced-motion insuffisante, test obsolete `reveal-button` encore present.
- Les commits par story ont garde une trace propre : 14.1 maquettes, 14.2 arrivee session-only, 14.3 retrait reveal.

### Points A Surveiller

- La variante B est traduite via la carte existante enrichie, pas comme une recreation pixel-perfect du ticket mockup. C'est volontaire pour eviter la duplication, mais l'ecart visuel devra etre juge en usage reel.
- `tribuneArrivalClosed` remplace `revealed` comme evenement de fin du moment d'arrivee pour le badge et le score supporter. C'est coherent avec le nouveau flux, mais il faudra surveiller que les recompenses restent intuitives.
- Le texte `Rejouer` existe encore dans le Donjon, hors flux classement. Les tests Story 14.3 couvrent l'absence dans le flux classement, pas une interdiction globale du mot.

### Actions De Suivi

- Verifier visuellement sur mobile Android que le bouton Fermer reste accessible avec le clavier virtuel et les safe areas.
- Lors de la prochaine evolution UI, preferer encore la reutilisation de composants metier existants avant de coder un mockup pixel-perfect.
- Si l'arrivee Tribune doit devenir plus proche du ticket valide, le faire comme une passe visuelle sur `tribune-arrival.css`, pas en dupliquant `score-card.js`.

## Next Epic Preparation

- Le classement est maintenant direct apres fermeture, donc toute story future qui touche `classements` doit partir de l'hypothese : pas de reveal manuel.
- Les tests d'app integration sont le meilleur point d'ancrage pour verifier les transitions de flux d'entree.
- Toute nouvelle animation d'entree/sortie doit explicitement prouver qu'elle respecte `prefers-reduced-motion`.
